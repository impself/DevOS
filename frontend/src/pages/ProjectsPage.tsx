import PlaceholderPage from "@/components/PlaceholderPage"
import { FolderKanban } from "lucide-react"

export default function ProjectsPage() {
  return (
    <PlaceholderPage
      icon={FolderKanban}
      title="Projects"
      description="Manage all projects across workspaces. Create, organize, and track project progress with Sprint management, task boards, and team collaboration."
    />
  )
}
