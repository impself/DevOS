import PlaceholderPage from "@/components/PlaceholderPage"
import { Users } from "lucide-react"

export default function TeamPage() {
  return (
    <PlaceholderPage
      icon={Users}
      title="Team"
      description="Manage team members, roles, and permissions. Assign developers to projects, set access levels, and track contributions across the workspace."
    />
  )
}
