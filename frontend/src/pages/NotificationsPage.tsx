import PlaceholderPage from "@/components/PlaceholderPage"
import { Bell } from "lucide-react"

export default function NotificationsPage() {
  return (
    <PlaceholderPage
      icon={Bell}
      title="Notifications"
      description="Stay updated with project activities, task assignments, document mentions, code reviews, and system alerts. Configure notification preferences per project."
    />
  )
}
