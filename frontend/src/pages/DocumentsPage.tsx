import PlaceholderPage from "@/components/PlaceholderPage"
import { FileText } from "lucide-react"

export default function DocumentsPage() {
  return (
    <PlaceholderPage
      icon={FileText}
      title="Documents"
      description="Browse and manage all documents across projects. Rich text editing with real-time collaboration, version history, and AI-powered writing assistance."
    />
  )
}
