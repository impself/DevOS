import PlaceholderPage from "@/components/PlaceholderPage"
import { Search } from "lucide-react"

export default function SearchPage() {
  return (
    <PlaceholderPage
      icon={Search}
      title="Search"
      description="Full-text search across projects, documents, code, tasks, and knowledge base. Find anything instantly with semantic and keyword search."
    />
  )
}
